

# Условие
Сделать базу данных

- Придумать условие
- Придумать набор сущностей
- Создать таблицы (название таблицы, атрибут и его тип данных) (1 sql фай)
- 2 SQL файл, alter table
- 3 файл copy.csv


```bash
# bmstu-db
python3 -m venv .venv
source ./.venv/bin/activate
pip install -r requirements.txt

# Установили локальное окружение, можно поднимать базу данных

docker-compose up -d
```