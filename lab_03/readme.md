# ЛР3 Разные функции на SQL


```bash
# Как запускать
# из рута
python3 -m venv .venv
source ./.venv/bin/activate
pip install -r requirements.txt

# Установили локальное окружение, можно поднимать базу данных
cd lab_02
docker-compose up -d

# Переходим в паапку для работы
cd ../lab_03
```