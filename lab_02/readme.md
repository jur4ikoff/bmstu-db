

# Условие
Написать 25 запросов

# Правки
- Связать все через foreign key
- Сделать норм генератор

```bash
# bmstu-db
python3 -m venv .venv
source ./.venv/bin/activate
pip install -r requirements.txt

# Установили локальное окружение, можно поднимать базу данных

docker-compose up -d
```